import { NavbarRoutes } from "@/components/navbar-routes";

const Navbar = () => {
  return (
    <div className="p-4 border-b h-full bg-white shadow-sm">
      <NavbarRoutes />
    </div>
  );
};

export default Navbar;
